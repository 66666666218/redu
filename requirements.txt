# ---- 核心 ----
requests>=2.31,<3
# 闲鱼 mtop 传输层:模拟 Chrome TLS/HTTP2 指纹,纯协议下更不易被风控标记(见 doc/dev.md §5.8)
curl-cffi>=0.16.3
numpy>=1.26,<2
APScheduler>=3.10,<4
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
python-dotenv>=1.0

# ---- 监控接口(可选) ----
fastapi>=0.110
uvicorn[standard]>=0.29
# ---- 鉴权 ----
# 直接用官方 bcrypt,不用 passlib:passlib 1.7.4(2020 年停更)会读 bcrypt.__about__,
# 该属性在 bcrypt 4.1+ 已移除,导致 bcrypt 5.x 下注册/登录全部 500。
bcrypt>=4.0,<6
pyjwt>=2.8
python-multipart>=0.0.9

# ---- 数据库(MySQL) ----
sqlalchemy>=2.0
pymysql>=1.1
cryptography>=42

# ---- 开发/测试 ----
pytest>=8.0
ruff>=0.4
mypy>=1.8
# 仅 scripts/probe_*.py 抓包探测用(核对接口是否仍免签名);运行时采集已全量直连,无需浏览器
playwright>=1.42
